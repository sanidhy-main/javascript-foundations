const lit = document.createElement('li');
lit.textContent = 'Hello World!';

list.appendChild(lit);
// list.removeChild(lit);

lit.textContent = 'Hello JavaScript';

lit.style.color = 'blue';
lit.style.fontWeight = 'bold';

//Commonly Used Tags
    //div -- general container
    //span -- inline container
    //p -- paragraph
    //h1 - h6 -- headings (h1 - biggest, h6 - smallest)
    //ul -- unordered list
    //li -- list item (must be inside <ul>)
    //button -- clickable button
    //input -- input
    //form -- used with inputs
    //img  -- image (sure img.src = "" to set a value)
